{% assign title = settings.why_title %}

<div id="why-thumbnails" class="why-thumbnails">
  <h2 class="why-thumbnails-heading">
    {{ title }}
  </h2>

  {% for num in (1..10) %}
    {% include "why-thumbnail" %}
  {% endfor %}
</div>
